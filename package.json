{
  "name": "struct",
  "private": true,
  "version": "0.1.0",
  "description": "Authenticated binary telemetry gateway and device SDKs",
  "scripts": {
    "dev:web": "npm --prefix web run dev",
    "dev:tcp": "npm --prefix tcp-server run dev",
    "build:web": "npm --prefix web run build",
    "start:tcp": "npm --prefix tcp-server run start",
    "test": "npm --prefix web test && npm --prefix tcp-server test && node tests/database.cjs && node tests/delivery.cjs && node tcp-server/test-delivery.js && node sdk/tests/test_vectors.cjs && node sdk/tests/test_udp.cjs",
    "test:database": "node tests/database.cjs",
    "test:sdk": "node sdk/tests/test_udp.cjs",
    "test:protocol": "node sdk/tests/test_vectors.cjs",
    "package:sdk": "python scripts/package-sdk.py"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  }
}
